No code, no shell — expert synthesis only, <220 words. Phase 1d is IDLE and confirmed NO-GO (mesh-free FAM-C 0.637 chair vs 0.72 bar; path B dead). We converge to path C. My adversarial partner argues: (a) do NOT run a 2-scene multi-scene probe (chair->lego 0.82 transfer) — it is a scope trap that invites an Objaverse benchmark demand and dilutes the clean negative; (b) the #1 hostile-reviewer attack on path C is "consistent GARBAGE" — the 7-13x temporal-coherence win is a trivial byproduct of object-space parameterization and useless because precision stays texture-polluted; (c) the cheapest preempt is a PRECISION-COHERENCE PARETO FRONTIER: sweep our lines' threshold vs 2D Canny/PiDiNet and show that at MATCHED precision the object-space coherence gap persists, proving the temporal win is orthogonal to the supervision ceiling. Two questions: (1) Do you agree path B is dead, or is the 2-scene probe worth one cheap held-out shot? (2) Is the matched-precision Pareto frontier the right #1 defense figure, and is it computable from ALREADY-BANKED artifacts (the temporal-coherence eval clouds + per-frame Canny/PiDiNet baselines from Track N/O/P)? Flag any TRAP in it — e.g. does our coherence win partly come from keeping FEWER/different lines, such that matching precision could erase the gap? Converge on ONE next deliverable with a frozen gate.
