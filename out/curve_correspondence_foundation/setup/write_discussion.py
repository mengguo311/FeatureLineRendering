"""Render detailed tables directly from immutable measured values."""
import pathlib,sys
O=pathlib.Path(__file__).resolve().parents[1];sys.path.insert(0,str(O/'code'))
from cc_io import read_json
r=read_json(O/'results.json')
lines=['# Scientific interpretation','', '**STOP_CORRESPONDENCE for Lego and Chair.** The explicit correspondence formulation produces some persistent 3D curves, unlike the corrected local-pixel baseline, but does not establish the registered foundation hypothesis. Both primary arms fail G1–G4 in both scenes, and neither scene meets stable GS benefit. These machine failures determine STOP without independent manual review.','', 'The one formulation is unique calibrated epipolar-intersection matching of ordered Canny segments, fixed bilateral RGB evidence, mutual ambiguity margins, closed three-view identities, monotone arc maps, robust triangulation and bounded shared-polyline bundle adjustment. Identity is frozen before fitting. The GS arm only vetoes unsupported spans; it never invents an identity.','', 'The preregistration and literature memo were committed and pushed before execution. The targeted classical literature establishes that curve identity, confirmation views, ordering and bundle adjustment are not novel inventions. This experiment contributes a controlled feasibility/abstention result under fixed images and qualified GS priors. See LITERATURE.md for verified sources and reading limits.','', '## Yield and prediction','', 'Supported F length is averaged over all eight F views, counting a track only in its matched observation views and counting zero in the others. C/DEV prediction lengths are means over all held-out views. Joint support requires distance <=2px and unoriented tangent error <=20 degrees; unknown directions count against it. Detector proximity is not ground-truth correspondence accuracy.','', '| Scene / arm | Tracks | Separated | F supported length (px/view) | Four-view fraction | C joint | DEV joint | C independent overlap (forward/backward) |','|---|---:|---:|---:|---:|---:|---:|---|']
for s in r['scenes'][:2]:
 for arm in ['image_only','gs']:
  y=s['yield_metrics'][arm];p=s['heldout'][arm];c=s['repeatability'][arm]['C_independent']
  lines.append(f"| {s['scene']} / {arm} | {y['track_count']} | {y['separated_tracks']} | {y['F_projected_length']:.3f} | {y['four_view_fraction']:.3f} | {p['C']['joint_fraction']:.3f} | {p['DEV']['joint_fraction']:.3f} | {c['forward']:.3f}/{c['backward']:.3f} |")
lines += ['', 'Every primary track has three views; none reaches four views. The gates require at least 12 spatially separated tracks, 300 supported F pixels/view, six occupied cells in four views and at least 25% four-view tracks. F residual/tangent fits pass for the few accepted curves, but fitting the generating images does not establish prediction.','', '| Scene / arm / split | Distance median/P90 (px) | Tangent median/P90 (deg) | Track coverage | Good/bad length (px/view) |','|---|---|---|---:|---|']
for s in r['scenes'][:2]:
 for arm in ['image_only','gs']:
  for split in ['C','DEV']:
   p=s['heldout'][arm][split]
   lines.append(f"| {s['scene']} / {arm} / {split} | {p['distance_median']:.3f}/{p['distance_p90']:.3f} | {p['angle_median']:.3f}/{p['angle_p90']:.3f} | {p['track_coverage']:.3f} | {p['supported_length']:.3f}/{p['unsupported_length']:.3f} |")
lines += ['', 'Chair GS passes the DEV prediction sub-gate, but fails C prediction and the other necessary gates. No average or favorable view rescues either scene. The all-in-frame gate conservatively includes occluded spans. Visibility-stratified sample counts and projected lengths are in visibility/SCENE; they cannot change this gate.','', '## Correspondence controls','', '| Scene / arm | F curves | C joint | DEV joint | C good length | DEV good length |','|---|---:|---:|---:|---:|---:|']
for s in r['scenes'][:2]:
 for arm in ['shifted','shifted_gs','random_graph','random_graph_gs','pairwise','pairwise_gs','no_order','no_order_gs']:
  p=s['heldout'][arm]
  lines.append(f"| {s['scene']} / {arm} | {s['arm_counts'][arm]} | {p['C']['joint_fraction']:.3f} | {p['DEV']['joint_fraction']:.3f} | {p['C']['supported_length']:.3f} | {p['DEV']['supported_length']:.3f} |")
lines += ['', 'Shifted and randomized associations retain their input counts but yield zero curves. This rejects those nulls. It does not pass G4: disabling order remains comparable by the frozen supported-length/precision criterion. Chair’s GS no-order output is identical to its primary GS output. Pairwise-only fitting produces many more curves and substantial unsupported clutter; its large good-length totals cannot certify correct persistent identities.','', 'All geometrically proposed pairs, ambiguous competitors, triangle/order failures and rejected fits remain in compressed records. Deterministic diagnostic panels show both selected pairs and competing curves. No successful track was hand-selected into the primary output.','', '## Re-estimation and GS contribution','', '| Scene / arm | Independent C curves | LOO passes / 8 | Qualified dose passes | Independent seed |','|---|---:|---:|---|---|']
for s in r['scenes'][:2]:
 for arm in ['image_only','gs']:
  reps=s['repeatability'][arm];loo=sum(v['passed'] for k,v in reps.items() if k.startswith('loo_'));dose=[v['passed'] for k,v in reps.items() if k.startswith(('moment_split','redistribute'))]
  seed='not eligible' if s['scene']=='lego' else ('PASS' if reps.get('seed_2718',{}).get('passed') else 'FAIL')
  if arm=='image_only':seed='prior independent by construction';doses='not a measured GS test'
  else:doses=f'{sum(dose)}/{len(dose)}'
  lines.append(f"| {s['scene']} / {arm} | {s['C_counts'][arm]} | {loo}/8 | {doses} | {seed} |")
lines += ['', 'Independent C reconstructions have zero bidirectional tube overlap with F in both arms and scenes. Most LOO re-estimations lose or replace supported spans. All nine controlled vetoes retain each scene’s single GS curve, but this stability is insufficient: retained coordinates are unchanged by construction. Chair seed2718 retains a shorter span; forward coverage is 0.631 and total length changes by 43.214%, exceeding the frozen 15% limit. Lego is CONTROLLED_ONLY because its second seed failed inherited replay calibration.','', 'The primary GS veto reduces unsupported DEV length but discards too much supported length: approximately 72.7% on Lego and 27.4% on Chair, versus a maximum permitted loss of 10%. It provides no stable net benefit under G5. Image-only also fails, so PIVOT_IMAGE_ONLY is not warranted. Drums and Ficus have no eligible parent and remain INSUFFICIENT_POSTERIOR_QUALITY; no stress test was executed on them.','', '## Visual and failure evidence','', 'Internal inspection of actual extraction, identity rejection, projection and comparison sheets shows extensive Canny fragmentation and sparse surviving spans. The Chair DEV42 overlay includes a short top-back contour fragment and a short lower-frame fragment; this does not establish six coherent shape spans or two internal structures. Pairwise reconstructions are visibly fragmented and cluttered; dense PCA linelets remain a visual reference, not curve truth. These observations are internal and unblinded, and are not a manual-gate pass.','', 'Explicit failure buckets retain silhouette, shadow, highlight, repeated-texture, junction and multilayer/cross-part diagnostics. Inherited boxes are coarse proxies, shadows/highlights overlap, and absent certified regions are marked unknown. No unsupported semantic error rate is claimed. Wrong depth and cross-part identity remain diagnostic hypotheses rather than mesh-verified truth.','', 'There are 2,030 canonical PNGs, fourteen 120-frame MP4s and 264 randomized review images. All reached primary/control arms have fixed F/C/DEV views; reached LOO and posterior arms have fixed-F sheets. Fixed-cardinality comparison uses one curve per nonempty arm. At the frozen 60%-of-minimum-F-ink budget, every correspondence arm misses the 5% tolerance; attempted ink sheets are retained and marked incomparable. Width and geometry were not modified to obtain a favorable comparison.','', 'Videos project fixed persistent geometry onto white backgrounds along the frozen orbit. They do not have new orbit GS visibility renders or RGB truth. The identity key is outside each blinded directory; there have been zero independent reviews. Independent visual review remains pending, but cannot reverse the failed necessary machine gates.','', '## Validity and scope','', 'TDD records 13 observed RED runs across 12 behavior slices, successful GREEN resolutions and passing refactor/full checks. The final targeted suite passes 45 tests and the unchanged repository suite passes 127. A contiguous-image drawing fix repaired the first Lego display attempt without changing any numerical metric/sample array. The first complete-suite wrapper failed runtime access/path checks and was corrected; every failed attempt remains. See EXECUTION_NOTES.md for exact boundaries.','', 'Native-open auditing includes pre-confinement byte hashing and the failed display attempt. TEST bytes stay sealed; no mesh enters generation, matching, fitting, parameter selection or evaluation. All eight posterior hashes and prior archives are checked again after the complete suite. Large arrays and traces are preserved server-side with exact inventory.','', 'The negative result is specific: a minimal unique-intersection, local-RGB, strict-segment-identity formulation cannot establish a useful repeatable foundation on both eligible scenes. It is not a proof that explicit cross-view curve correspondence is impossible. The observed bottlenecks include short/fragmented segment identity, ambiguous local appearance, strong order/common-support rejection and insufficient stability across independent view sets. No detector, threshold, dose or DEV-based repair was attempted, and no UDF/stroke system was built.']
with (O/'DISCUSSION.md').open('x') as f:f.write('\n'.join(lines)+'\n')
